"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UserSearchResults = void 0;
const UserStructure_1 = require("./UserStructure");
/** Represents search results for an AniList User. */
class UserSearchResults {
    constructor(json, client) {
        if (json.errors)
            throw new Error(JSON.stringify(json.errors));
        this.pageInfo = json.pageInfo;
        this.Results = json.Results.map((result) => new UserStructure_1.UserStructure(result, client));
    }
}
exports.UserSearchResults = UserSearchResults;
