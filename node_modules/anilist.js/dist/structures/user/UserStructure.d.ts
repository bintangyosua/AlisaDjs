import { Client } from '../..';
import { User, UserStatisticTypes } from '../../types/types';
/** Represents an AniList User. */
export declare class UserStructure {
    info: User;
    client?: Client;
    constructor(json: any, client: Client);
    /**
     * Follow this user.\
     * Requires you to be logged in.
     */
    follow(): Promise<any>;
    /** Get this user's AniList stats. */
    getStats(): Promise<UserStatisticTypes>;
}
