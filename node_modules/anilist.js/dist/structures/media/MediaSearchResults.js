"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MediaSearchResults = void 0;
const MediaStructure_1 = require("./MediaStructure");
/** Search results for an Anime or Manga. */
class MediaSearchResults {
    constructor(json, client) {
        if (json.errors)
            throw new Error(JSON.stringify(json.errors));
        this.pageInfo = json.pageInfo;
        this.Results = json.Results.map((result) => new MediaStructure_1.MediaStructure(result, client));
    }
}
exports.MediaSearchResults = MediaSearchResults;
